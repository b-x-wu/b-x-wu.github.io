---
import { Image } from "astro:assets";
import githubIcon from "~/assets/icons/github.svg";
import heartIcon from "~/assets/icons/heart.svg";
import linkedinIcon from "~/assets/icons/linkedin.svg";
import emailIcon from "~/assets/icons/mail.svg";
import twitterIcon from "~/assets/icons/twitter.svg";
import Divider from "~/components/core/Divider";
import IconLink from "~/components/core/IconLink";
import ResponsiveVisibility from "~/components/core/ResponsiveVisibility";

type Props = Record<string, never>;
---

<footer>
  <Divider color="muted" />
  <div class="tagLine">
    <span>made with</span>
    <Image
      class="heartIcon"
      id="footer-heart"
      src={heartIcon}
      alt="Love"
      height={24}
      width={24}
    />
    <ResponsiveVisibility>
      <span slot="xs">and a coffee</span>
      <span>and an oat milk cappuccino</span>
    </ResponsiveVisibility>
  </div>
  <div class="links">
    <IconLink
      href="https://github.com/b-x-wu"
      target="_blank"
      rel="noopener noreferrer"
      src={githubIcon}
      aria-label="Link to my GitHub profile"
      id="github-link"
      color="muted"
    />
    <IconLink
      href="https://twitter.com/b_x_wu"
      target="_blank"
      rel="noopener noreferrer"
      src={twitterIcon}
      aria-label="Link to my Twitter page"
      id="twitter-link"
      color="muted"
    />
    <IconLink
      href="https://www.linkedin.com/in/b-x-wu/"
      target="_blank"
      rel="noopener noreferrer"
      src={linkedinIcon}
      aria-label="Link to my LinkedIn page"
      id="linkedin-link"
      color="muted"
    />
    <IconLink
      href="mailto:bridgette.x.wu@gmail.com"
      target="_blank"
      rel="noopener noreferrer"
      src={emailIcon}
      aria-label="Link to email me"
      id="email-link"
      color="muted"
    />
  </div>
</footer>

<style>
  footer {
    flex-shrink: 1;
    color: var(--color-muted);
    display: flex;
    flex-direction: column;
    margin-bottom: 8px;
  }

  .tagLine {
    margin: 0 auto;
    display: flex;
    width: fit-content;
    flex-direction: row;
    text-align: center;
  }

  :global(.heartIcon) {
    width: 20px;
    height: 20px;
    margin: 0 8px;
  }

  .links {
    margin: 0 auto;
    display: flex;
    width: fit-content;
    flex-direction: row;
    gap: 8px;
    align-items: center;
    padding: 8px 0;
  }
</style>

<script>
  import heartIcon from "~/assets/icons/heart.svg";
  import heartDarkModeIcon from "~/assets/icons/heart-dark-mode.svg";
  import { getById } from "~/lib/dom";
  import { normalizeLocalStoreThemeMode, themeStore } from "~/lib/settings";

  const heartIconEle = getById<HTMLImageElement>("footer-heart");

  themeStore.subscribe((value) => {
    const theme = normalizeLocalStoreThemeMode(value);
    heartIconEle.src =
      theme === "light" ? heartIcon.src : heartDarkModeIcon.src;
  });
</script>
