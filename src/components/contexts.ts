import { createContext } from 'react';

export const DarkModeContext = createContext(false);
export const HighContrastModeContext = createContext(false);
export const ReducedMotionModeContext = createContext(false);

